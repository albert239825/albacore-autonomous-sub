# Albacore — Python dependencies (Jetson stack, laptop controller, scripts).
# Install from repository root: pip install -r requirements.txt

# NOTE:
#   PyTorch and TensorFlow on NVIDIA Jetson should NOT be installed from plain PyPI here.
#   On JetPack 6.2 / 6.2.1, install Jetson-matched wheels separately:
#   - PyTorch / torchvision / torchaudio: use NVIDIA Jetson AI Lab jp6/cu126 index
#   - TensorFlow: use NVIDIA's Jetson TensorFlow instructions / index for a compatible build
#   This avoids breaking installs on laptops and non-Jetson environments.
#
# pyserial     USB serial to Teensys
# numpy        arrays, audio/TDOA
# opencv-python camera + visualization in vision.detector
# ultralytics  YOLOv8 (downloads weights on first run)
# scipy        WAV I/O, resampling in classifier
# pygame       laptop/controller.py input

pyserial>=3.5
numpy>=1.24
opencv-python>=4.8
ultralytics>=8.0
scipy>=1.11
pygame>=2.5
